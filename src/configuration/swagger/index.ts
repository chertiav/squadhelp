import { ISwaggerConfig } from '../../common/interfaces/configuration/swagger';

export const ConfigSwagger: ISwaggerConfig = {
	title: 'Squadhelp api',
	description: 'This api for squadhelp',
	version: '1.0',
};
