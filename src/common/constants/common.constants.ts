import { ILogFile } from '../interfaces/constants';

export const LOG_FILE: ILogFile = {
	PATH: 'logs',
	NAME: 'errors.log',
	CURRENT_PATH: 'logs-current-day',
};

export const DEFAULT_AVATAR_NAME = 'anon.png';
